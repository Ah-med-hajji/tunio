/**
 * Production environment.
 * Replace the URLs below before building for production
 * (`ng build --configuration production` swaps this in via angular.json).
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.tuneo.tn',
  keycloakUrl: 'https://auth.tuneo.tn',
  keycloakRealm: 'TUNEO',
  keycloakClientId: 'tuneoproject',
};
